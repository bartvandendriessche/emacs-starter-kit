/**
 * Verify authentication of a user.
 *
 * Implementation of hook_auth
 *
 * The _auth hook is the heart of any authentication module. This
 * function is called whenever a user is attempting to log in using
 * your authentication module. The module uses this information to
 * allow or deny access to the site.
 *
 * @return For successful authentications, this function returns TRUE. Otherwise, it returns FALSE.
 */
function ${1:`(sacha/drupal-module-name)`}_auth($username, $password, $server) {
}
